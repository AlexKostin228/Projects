#include <iostream>
#include <cmath>

int main(){
    double x = 3.14;
    double y = 4;
    double z;
//max will return the greater of two values or variables
//    z = std::max(x, y);
//    z = std::min(x, y);
//    z = pow (2, 4); - pow (power)(2, 4) = 2^4
//    z = sqrt(9);- square root of 9
//    z = abs(-3); - absolute value (Модуль)
//    z = round (x); - rounds to integer
//    z = ceil(x); - ceiling rounds up
//    z = floor (x); - rounds down
    std::cout << z <<'\n';   

    return 0;
}